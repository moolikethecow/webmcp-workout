/** Namespaced tag helper kept at its original path; the tag vocabulary itself
 *  belongs to the memory system, which is not part of this repo. */
export const taggedKind = (kind: string): string => `kind:${kind}`
