'use client'

/**
 * Templates tab — the folder-grouped cards + builder live in
 * components/gym/templates/. This shell file re-exports it so GymShell's import
 * path stays stable (P1 placeholder replaced in P2b).
 */
export { default } from '@/components/gym/templates/TemplatesTab'
